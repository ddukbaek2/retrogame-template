//==============================================================================
// 포함 모듈 목록.
//==============================================================================
import { SampleGame } from "../games/sample/samplegame.js";


//==============================================================================
// 이 프로젝트가 만드는 게임.
//
// **한 프로젝트에 게임 한 편입니다.** 게임을 갈아 끼우려면 위의 들여오기와 아래의 한 줄만
// 고치면 됩니다. 색은 `src/game/identity.js` 에서 정합니다.
//
// 게임은 `src/games/<id>/<id>game.js` 에 `GameModule` 을 물려받아 씁니다.
// 자세한 것은 `docs/게임-모듈-규약.md` 를 보십시오.
//==============================================================================
/**
 * @param { object } scene
 * @returns { object }
 */
export function createGameModule(scene) {
	return new SampleGame(scene);
}
