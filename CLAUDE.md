# Claude 지침

이 저장소는 **옛 PC 게임의 결로 게임을 만드는 틀**입니다. `libs/vanilla.js` 엔진 위에 얹혀 있습니다.
1 ~ 8 비트 화면, 브라운관, 도트 글꼴, 방향키와 확인 취소만으로 노는 게임을 만듭니다.

**한 프로젝트에 게임 한 편입니다.** 이 틀을 복제해 게임 하나를 만듭니다. 여러 편을 한 앱에
담는 구조가 아닙니다. 게임은 `src/game/gameentry.js` 가 가리키는 하나뿐입니다.

## 문서 우선 (모든 세션 필수)
- **요청 처리, 빌드, 테스트, 배포의 단일 기준은 `docs/개발-프로세스.md` 입니다.** 매 세션 이 문서를 근거로 처리합니다. 프로세스가 바뀌면 이 문서를 먼저 갱신합니다.
- **새 환경에서 작업을 이어갈 때는 아래 문서를 먼저 읽습니다.**
  | 문서 | 다루는 것 |
  |---|---|
  | **`docs/게임-모듈-규약.md`** | **게임을 이 틀에 얹는 방법, 공통으로 지킬 것** |
  | `docs/게임-기본조건.md` | 해상도, 조작, 비주얼 방향 (바뀌면 거의 모든 코드가 영향을 받는 조건) |
  | `docs/UI-규약.md` | 화면 시각, 크기, 움직임 규약 |
  | `docs/리소스-규약.md` | 글꼴과 그 밖의 자산 |
  | `docs/개발-프로세스.md` | 실행, 빌드, 검증, 배포 절차 |
  | `docs/엔진-보완-내역.md` | `libs/vanilla.js` 에 가한 수정 (원본 리포 역이식용) |
- **토큰을 아낍니다.** 병렬 에이전트는 꼭 필요한 만큼만, 검증은 꼭 필요한 한 번씩만, 보고는 짧게.

## 문장 부호 방침
- **가운뎃점(·)과 줄표(—)를 쓰지 않습니다.** 사용자에게 하는 설명, 코드 주석, 문서, 화면에 나오는 문구, 커밋 메시지, 작업 로그 어디에도 쓰지 않습니다.
- 대신 쉼표, 가운데 빈칸, 슬래시(/), 괄호, 줄바꿈을 씁니다. 목록은 목록으로 씁니다.

## 어휘 방침
- **한자어를 피하지 않습니다.** 언어, 위치, 종류, 상태, 설정, 장착, 해제처럼 보통 쓰는 말을 그대로 씁니다. 순우리말로 억지로 바꾸지 않습니다.
- 화면 문구, 설명, 주석, 문서 어디에나 같습니다.

## 응답 언어
- 항상 한국어로만 응답합니다.

## 존댓말 방침
- **쓰는 모든 글은 존댓말입니다.** 사용자에게 하는 설명, 코드 주석(파일 머리, 함수 헤더, 인라인 전부), `docs/` 의 모든 문서, 화면에 나오는 모든 문구, 커밋 메시지, 작업 로그.
- `~한다` `~이다` `~하자` 같은 서술체, 청유체를 쓰지 않습니다. `~합니다` `~입니다` 로 씁니다.
- 표의 항목 이름이나 짧은 명사구는 그대로 두어도 됩니다. **문장이면 존댓말입니다.**
- **캐릭터 대사는 예외입니다.** 그 인물의 말투를 따릅니다. 안내 문구, 로그, 메뉴, 설명은 그대로 존댓말입니다.

## 구현 품질 방침
- 컴포넌트나 기능을 구현할 때 실제 사용 가능한 수준의 스펙을 갖추도록 고민합니다.
- 경계 조건(아이템 0 개, 크기 불균일, 가변 크기 등)을 고려한 로직을 작성합니다.
- 특수 케이스가 아닌 일반 케이스를 기본으로 설계하고, 특수 동작은 setter 로 제공합니다.
- 내부 계산은 외부 하드코딩 상수에 의존하지 않고 런타임에 실제 데이터를 읽어 처리합니다.

## 코딩 방침
- 가지의 정합성을 맞추기 위해 뿌리 부분의 정합성을 훼손하지 않습니다. 파생 클래스나 상위 계층의 문제를 해결하려고 기반 클래스나 하위 계층의 설계를 변경해서는 안 됩니다.
- 기존 코드의 표기법(네이밍 컨벤션, 들여쓰기, 주석 스타일 등)을 그대로 따릅니다.
- 코딩 요청 시 필요한 부분만 최소한으로 수정합니다. 불필요한 리팩토링, 추가 기능, 스타일 변경을 하지 않습니다.
- `libs/vanilla.js` 에 있는 기존의 코딩 스타일을 무조건 따릅니다.
- `assets/` 안의 자산 구조를 일관되게 유지합니다.
- 프로젝트 내에 존재하지 않는 가상의 함수나 클래스를 창작하여 사용하지 않습니다. 반드시 제공된 코드베이스 내의 요소만 사용합니다.
- **엔진(`libs/vanilla.js`)은 git 서브모듈입니다.** 고쳐야 할 것이 생기면 **원본 리포에 일반적인 옵션으로** 넣고 서브모듈을 올립니다. 특정 게임을 위한 이름이나 동작을 엔진에 박지 않습니다. 고쳤으면 `docs/엔진-보완-내역.md` 에 적습니다.

## 필수 코딩 스타일
- **변수명 유지 및 축약 금지:** 기존 코드의 변수명을 임의로 변경하지 않습니다. 변수명을 줄여 쓰거나 약어를 사용하는 것을 금지합니다. (예: `canvasRenderingContext` 를 `ctx` 로 축약 금지, `button` 을 `btn`, `value` 를 `val` 로 축약 금지) 이 규칙은 변수명뿐 아니라 UI 레이블, 버튼 텍스트 등 화면에 표시되는 모든 문자열에도 동일하게 적용합니다.
- **지역 변수 할당 강제:** `get` 메서드나 프로퍼티의 반환값을 다른 함수의 인자로 직접 전달하지 않습니다. 반드시 풀네임의 지역 변수에 먼저 할당한 후 사용합니다.
- **다중 구문 작성 금지:** 한 줄에 세미콜론(`;`)을 하나 초과하여 사용하지 않습니다.
- **중괄호 강제:** 모든 조건문, 반복문, 함수는 반드시 중괄호 `{}` 를 사용합니다. 1 줄이라도 생략을 금지합니다. 여는 중괄호는 시작행의 끝에, 닫는 중괄호는 새로운 행에 위치해야 합니다.
- **조건문 개행 강제:** `else`, `else if`, `catch`, `finally` 등은 반드시 다음 행에서 시작합니다.
- **switch 문 중괄호 강제:** 모든 `case` 와 `default` 블록은 반드시 중괄호를 사용합니다. `break;` 는 반드시 해당 블록 내부에 위치해야 합니다.
- **Private 멤버 변수:** 모든 클래스의 `private` 멤버 변수는 반드시 `#` 접두사를 사용합니다. 단, `private` 메서드는 사용하지 않습니다. 메서드는 항상 공개 또는 일반 메서드로 작성합니다.
- **Private 변수 접근 강제:** 프라이빗 변수의 값이 필요할 때는 반드시 getter 메서드를 통해서만 접근합니다.
- **주석 및 스타일 보존:** JSDoc, 함수 헤더 주석(`//=================`), 인라인 주석 등 모든 주석과 기존 코딩 스타일을 삭제하거나 임의로 수정하지 않습니다.
- **변경 이력 주석 금지:** '무엇을 변경했다' 는 식의 주석을 코드 내에 추가하지 않습니다.
- **전역 내장 객체 접근 강제:** `Object`, `Promise`, `Math`, `JSON`, `Array` 등을 사용할 때는 반드시 `System.` 접두사를 붙입니다. 파일 상단에 `const System = globalThis;` 선언이 있는 경우에 적용합니다.
- **문자열 따옴표 통일:** 쌍따옴표(`"`) 또는 템플릿 리터럴만 사용합니다. 홑따옴표는 사용하지 않습니다.

## 빌드 검증 방침
- 코드를 조금 고칠 때마다 빌드를 돌리지 않습니다.
- 단, **작업을 마무리할 때는 `docs/개발-프로세스.md` 의 워크플로대로 반드시 빌드하고 헤드리스 크롬으로 오류 0 을 확인합니다.**

## 비주얼 방침
- **1 ~ 8 비트 화면입니다.** 색 수는 `src/game/display.js` 가 맡습니다. 화면 코드가 `Colors` 의 열쇠로 말한 색을, 게임의 되짚기를 거친 뒤 지금 설정한 색 수(1, 2, 4, 8 비트)로 바꿔 돌려줍니다. 그리는 쪽은 그 값만 씁니다.
  - **모니터 색은 여덟입니다.** 1 비트 녹색, 흰색, 적색, 청색, 2 비트 녹색, 흰색, 4 비트 16 색, 8 비트 256 색. F9 가 앞으로, F10 이 뒤로 돌립니다. 16 색, 256 색은 VGA 기본 팔레트(`src/game/vgapalette.js`)입니다.
  - **말은 "녹색" 과 "흰색" 입니다.** "초록" 이라 쓰지 않습니다.
- **브라운관 모니터.** 창 전체를 덮는 캔버스가 모니터 그림(`assets/monitor/bezel.png`)을 그리고, 화면 구멍에 게임을 볼록 렌즈로 휘어 보여 줍니다(`src/game/crt.js`). 주사선, 형광 글로우, 형광 마스크가 얹힙니다. 게임 코드 밖의 후처리라 게임이 그리는 것과 무관합니다. 설정 "브라운관" 으로 끕니다.
  - 브라운관 설정은 둘입니다. 모니터 프레임 보기, 볼록 효과 사용. 따로 켜고 끕니다.
  - **브라운관을 꺼도 색 수는 그대로입니다.** 필터는 색에 손대지 않습니다.
- **도트 느낌이 필수입니다.** 글꼴은 도트 글꼴이고 설계 크기의 정수 배로만 씁니다. 백버퍼를 기준 해상도로 고정해 창에는 정수 배율로만 키웁니다(`src/game/viewscale.js`). 엔진의 글자 안티앨리어싱을 꺼서 구운 글자의 가장자리를 걷어 냅니다.
- **글자를 늘리거나 줄이지 않습니다.** 움직임은 옮기기와 색뿐입니다. 배율 트윈은 도트를 깨뜨립니다.
- **그림은 밝기 네 단의 마스크로 굽습니다.** 화면의 색 수가 설정마다 바뀌므로 받은 색을 그대로 쓰지 않습니다. `tools/bake-sprites.py` 로 구워 두고 그리는 쪽이 `Colors` 의 열쇠로 물들여 찍습니다(`src/game/sprite.js`). 색은 늘 `palette.js` 를 지나므로 1, 2, 4, 8 비트 어디서도 어긋나지 않습니다.
- **런처(`launcher.html`, `webtemplate/index.html`)는 표준 틀로 둡니다.** 캔버스가 `100vw × 100vh` 이고, 브라운관과 모니터 테두리는 런처와 무관한 개념입니다. 도트 보간 끄기, 캔버스 자리 잡기는 `src/game/devicesize.js` 가 코드로 합니다.

## 조작 방침
- **입력은 방향 넷 + 확인 + 취소 (+ 메뉴) 가 기본입니다.** 키보드는 방향키, WASD / Enter, Space, Z / Backspace, X / ESC, 게임패드는 십자키, 왼쪽 스틱 / A / B / Start. (`src/game/command.js`)
- **마우스는 왼쪽이 확인, 오른쪽이 취소입니다.** 오른쪽 단추의 브라우저 기본 메뉴는 막습니다. 휠과 호버는 쓰지 않습니다.
- **마우스, 손가락으로 항목을 눌러 고를 수 있습니다.** 창 좌표를 렌즈 왜곡까지 되짚어 게임 좌표로 바꾸고(`crt.js` 의 `mapWindowPointToGame`), 그 자리의 목록 항목을 고릅니다.
- **손가락은 됩니다.** 끌면 그 방향을 누르고 있는 것, 톡 치면 확인, 두 손가락은 취소, 길게 누르면 메뉴입니다.
- **손가락으로만 노는 기기에는 모니터 아래에 가상 게임패드가 섭니다.** 게임 화면과 별개입니다. 십자키와 A(왼쪽, 확인), B(오른쪽, 취소), SELECT, START 입니다. (`src/game/virtualpad.js`)
- **타이핑이 필요한 편은 `src/game/typing.js` 를 씁니다.** (숨긴 input, 한글 IME, 이력, Tab 채우기) 치는 동안 명령 읽기는 ESC 만 명령으로 봅니다.
- 방향은 누르고 있으면 반복됩니다. (0.26 초 뒤 0.085 초 간격) 확인, 취소는 누른 순간 한 번입니다.

## 게임 모듈 방침
- 게임 = `src/games/<id>/` 의 모듈(`GameModule` 파생) + 로직 + 화면 + 내용 표. 얹는 방법과 공통 규칙은 `docs/게임-모듈-규약.md` 가 단일 기준입니다. `src/game/gameentry.js` 가 그 모듈을 만들고, `src/game/identity.js` 가 색을 정합니다.
- **규칙은 순수 로직입니다.** `src/games/<id>/logic/` 은 엔진, 브라우저 API 를 들여오지 않습니다. Node 의 도구, 검증이 같은 파일을 씁니다(`logic/package.json` 이 그 폴더를 ESM 으로 선언).
- **게임이 제 구성, 제 조작 방식을 가집니다.** 공통 틀을 주 화면으로 쓰지 않습니다. 그 게임이 필요로 하는 화면을 만듭니다.
- 타이틀에서 **시작**을 고르면 바로 게임으로 들어갑니다. 중간에 고르는 화면이 없습니다.

## 테마 방침
- **색의 뜻은 `Colors` 의 열 가지 남짓이 전부입니다.** 화면 코드는 색 리터럴을 쓰지 않고 `Colors` 의 열쇠만 씁니다. (`docs/UI-규약.md`)
- **이 게임의 종이 색, 먹 색, 강조색은 `src/game/identity.js` 가 정합니다.** 앱이 게임에 들어갈 때 `Colors` 의 값을 되짚어 칩니다(`palette.js` 의 `setPaletteRemap`). 흐린, 옅은 글자와 정보, 완료, 오류 색은 종이의 밝기에서 자동으로 나옵니다.
- **`Colors` 의 값은 뜻이 다르면 서로 겹치지 않아야 합니다.** 열쇠가 곧 값이라, 값이 겹치면 둘을 갈라낼 수 없습니다.

## UI 배치 방침
- **화면은 엔진의 노드 트리입니다.** 화면 하나(`src/ui/screennode.js` 파생)가 루트 아래에 놓이고 한 번에 하나만 켜집니다. 메뉴, 설정, 규칙도 겹창이 아니라 화면입니다. 게임의 화면은 `"<게임 id>:<이름>"` 으로 등록됩니다.
  자리는 `src/ui/layout.js` 의 `placeNode` 로 잡고, 글자는 `src/game/text.js` 의 `drawText` 로 그립니다.
- **공용 부품은 `src/ui/`**, **타이틀은 `src/screen/`**, **설정은 `src/panel/`**, **게임 화면은 `src/games/<id>/screen/`** 입니다.
- **UI 시각, 크기 규약(색, 글자 티어 `UiFontSize`, 칸 크기, 화면 구성)은 `docs/UI-규약.md` 를 단일 기준으로 따릅니다.** 화면 코드에 글자 크기, 색 리터럴을 쓰지 않습니다.

## UI 조작감 방침
- **모든 상태 변화는 움직임으로 말합니다.** 고른 항목은 오른쪽으로 밀리고, 글자는 새 자리로 미끄러지고(`approachValue`), 막히면 흔들립니다.
- **알파 트윈은 쓰지 않습니다.** 옅어지며 나타나거나 사라지는 연출이 없습니다. 색은 바뀌지만 투명도는 바뀌지 않습니다.
- **배율 트윈도 쓰지 않습니다.** 도트가 깨집니다. 움직임은 옮기기와 색뿐입니다.
- **밋밋하게 만들지 않습니다.** 이징(`src/game/easing.js`)으로 펀치감을 줍니다. 소리(`src/game/beep.js`, 합성 효과음)가 손맛을 거듭니다.
- **연출은 짧고 경쾌하게.** 단순하고 빠르고 쾌적하며 짧게 치는 느낌이어야 합니다.

## UI 문구 방침
- **요청하지 않은 홍보 문구, 캐치프레이즈, 부제, 안내 문구를 UI 에 임의로 추가하지 않습니다.**
- **글자는 필요한 곳에만 넣습니다.**
- **화면의 글자는 서로 겹치지 않습니다.** 겹칠 여지가 있으면 위치를 계산으로 밀어 냅니다.
- 어휘는 `docs/UI-규약.md` 를 따릅니다.

## 작업 로그 방침
- `.logs/claude-YYYY-MM-DD.log` 형식으로 `.logs` 디렉토리에 날짜당 하나의 로그 파일만 유지합니다. 동일 날짜 파일이 이미 존재하면 이어서 기록합니다.
- 각 작업 항목에 요청 타임스탬프와 응답 타임스탬프를 `[HH:MM 요청 → HH:MM 응답]` 형식으로 기록합니다.
- 번호 붙은 분할 로그(`-2`, `-3` 등)는 만들지 않습니다.
